'use client';

import React from 'react';
import { TripUuid } from '@/lib/types';

export default function ExpensesView( {tripUUID} : TripUuid ) {
    console.log(tripUUID);

    return (
        <div>ExpensesView</div>
    )
}

